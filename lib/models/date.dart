class DateEvent {
  final String title;
  final String description;
  final String imagePath; // Ruta del activo (assets/images/aniversario.png)

  const DateEvent(this.title, this.description, this.imagePath);
}
